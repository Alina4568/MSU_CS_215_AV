#include <stdio.h>
#define N 10


void print(int a[], int n)
{
    for(int i=0; i < n; i++)
        printf("%d ", a[i]);
    printf("\n");
}

void swap(int *x, int *y) // смена двух элементов местами
{
    int tmp;
    tmp = *x;
    *x = *y;
    *y = tmp;
}

void sort_bubble(int a[], int n)
{
    int noSwap;
    for(int i=n-1; i >= 0; i--)
    {
        noSwap = 1;
        for(int j=0; j < i; j++)
        {
            if(a[j] > a[j+1]) 
            {
                swap(&a[j], &a[j+1]);
                noSwap = 0;
            }
        }
        if(noSwap == 1) break;
    }
}


int main()
{
    //int arr[N] = {3,6,1,8,4,9,0,2,7,5};
    int n;
    scanf("%d", &n);
    int arr[n];
    
    for(int i = 0; i < n; i++){
        scanf("%d", &arr[i]);
    }
    
    sort_bubble(arr, n);
    
    print(arr, n);

    return 0;
}
